---

const {url, title , icon} = Astro.props;
---

<a href={url} target="_blank" class="w-full h-full flex flex-col items-center justify-center p-2 group">
    <iconify-icon class="text-violet-600 text-[5rem] group-hover:scale-110  transition-transform" icon={icon} ></iconify-icon>
    <h3 class="text-text font-bold text-sm text-center">{title}</h3>
</area>