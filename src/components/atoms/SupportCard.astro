---

const {url, title , icon} = Astro.props;
---

<a href={url} target="_blank" class="w-full h-full flex flex-col items-center justify-center p-2">
    <iconify-icon class="text-violet-600 text-[5rem]" icon={icon} ></iconify-icon>
    <h3 class="text-text font-bold text-sm text-center">{title}</h3>
</area>