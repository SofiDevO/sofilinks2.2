---
const { title, class:className, ...props} = Astro.props;

---


<h2 class:list={`text-center bg-gradient-to-r from-caracol to-celeste bg-clip-text  text-4xl lg:text-5xl font-bold text-transparent my-4 ${className}`} {...props}>{title}</h2>