---
const { link="/", direction,  class: className , transition, ...res } = Astro.props;
import Icon from "@atoms/icons/Icon.astro";
---

<a href={link} class={className}   transition:name={transition}   {...res}>
    <slot name="arrow-content" />

    {
        direction === "left" ? (
        <Icon name="stash:arrow-left-solid " class="text-caracol" />
    ) : (
        <Icon name="stash:arrow-right-solid" class="rotate-180 text-caracol" />
    )
  }
</a>
